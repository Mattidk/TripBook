<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="72dp"
    android:paddingRight="72dp"
    android:paddingTop="140dp"
    android:background="@drawable/bg_animation"
    tools:context="dk.mathiaspedersen.tripbook.presentation.activity.LoginActivity">

    <TextView
        android:id="@+id/logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="TripBook"
        android:textColor="#FFFFFF"
        android:textSize="48sp"
        android:layout_marginBottom="70dp"
        android:layout_centerHorizontal="true"/>

    <Button
        android:id="@+id/google_provider"
        style="@style/TripBook.Button.AccountChooser.GoogleButton"
        android:text="@string/sign_up_with_google"
        tools:visibility="visible"
        android:layout_below="@id/logo"/>

    <Button
        android:id="@+id/email_provider"
        style="@style/TripBook.Button.AccountChooser.EmailButton"
        android:text="@string/sign_up_with_google"
        tools:visibility="visible"
        android:layout_below="@+id/google_provider"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Login"
        android:layout_below="@id/email_provider"
        android:textSize="16sp"
        android:layout_centerHorizontal="true"
        android:textColor="#FFFFFF"/>

</RelativeLayout>


    <!--android:paddingBottom="@dimen/activity_vertical_margin"-->
    <!--android:paddingLeft="@dimen/activity_horizontal_margin"-->
    <!--android:paddingRight="@dimen/activity_horizontal_margin"-->
    <!--android:paddingTop="@dimen/activity_vertical_margin"-->